# Build + dist
/build/
/dist/
/out/
/pkg/

# Cyrius vendored deps — regenerated by `cyrius deps` from cyrius.cyml.
# Tracked: cyrius.cyml [deps] + cyrius.lock if/when one is produced.
/lib/

# Release / toolchain artifacts
cyrius-*.tar.gz
*.tar.gz
SHA256SUMS
SHA256SUMS.*

# Local runtime
*.core
.claude/

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Secrets (never commit)
.env
.env.*
*.pem
*.key
